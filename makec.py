import numpy as np
import pandas as pd

#rng = np.random.default_rng()

#users = np.arange(10)
#n = 50
#y = rng.choice(a=[-1, 1], size=(50, ), p=[.5, .5])
#W = rng.choice(a=[0, 1], size=(len(users), n), p=[.65, .35])

#





print(c)
print(test)